# Notes for AI agents

## Package management
Use `uv pip install` instead of pip.

## Running
All Python commands (run, install, scripts) should be executed via `uv` to ensure the uv virtual environment is used. Use `uv run python ...` or `uv run <script>` instead of plain `python`.
